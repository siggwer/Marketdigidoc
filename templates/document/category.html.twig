{% extends 'base.html.twig' %}

{% block body %}

<div class="gtco-loader"></div>
	
<div id="page">

	<!-- NAVIGATION -->
	{% include "nav/nav.html.twig" %}
	
	<!-- HEADER -->
	<header id="gtco-header" class="gtco-cover gtco-cover-sm" role="banner" style="background-image:url(/images/Img_document_marketing_1.png);">
		<div class="gtco-container">
			<div class="row">
				<div class="col-md-8 col-md-offset-2 text-center">
					<div class="display-t">
						<div class="display-tc animate-box" data-animate-effect="fadeIn">
							<h1></h1>
							<h2>
							<a href="#" target="_blank"></a></h2>
						</div>
					</div>
				</div>
			</div>
		</div>
	</header>

	<!-- DOCUMENT VIEW -->
	{# <div id="gtco-features">
			<div class="gtco-container">
				<div class="row">
				{% for document in documents %}
					<div class="col-md-4 col-sm-4 animate-box documents" data-animate-effect="fadeIn">
						<div class="feature-center">
							<span class="icon">
								{% if document.category.id == 1 %}
								<i class="icon-eye"></i>
								{% elseif document.category.id == 2 %}
								<i class="icon-command"></i>
                        		{% elseif document.category.id == 3 %}
								<i class="icon-pencil"></i>
                        		{% elseif document.category.id == 4 %}
								<i class="icon-laptop"></i>        
								{% endif %}		
								</span>
							<h3>{{ document.category.name }}</h3>
							<p><a href="{{ path("document_list", { 'category': document.category.id }) }}" class="btn btn-primary">Voir les documents</a></p>
						</div>
					</div>
				{% endfor %}
				</div>	
			</div>
		</div> #}

		{# <div class="text-center">
			<button class="btn btn-info" id="load-more">Voir plus</button>
		</div> #}

	<!-- FOOTER -->
	{% include "footer/footer.html.twig" %}

</div>	

{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="../js/load.more.js"></script>
{% endblock %}
